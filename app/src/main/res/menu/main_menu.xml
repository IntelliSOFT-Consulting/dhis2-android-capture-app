<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <group
        android:id="@+id/main_sections"
        android:checkableBehavior="single">
        <item
            android:id="@+id/menu_home"
            android:icon="@drawable/ic_menu_home"
            android:title="@string/menu_home_data" />
        <item
            android:id="@+id/menu_home_visualizations"
            android:visible="false"
            android:icon="@drawable/ic_menu_home"
            android:title="@string/menu_home_data" />
    </group>

    <group
        android:id="@+id/close_section"
        android:checkableBehavior="single">

        <item
            android:id="@+id/sync_manager"
            android:icon="@drawable/ic_menu_settings"
            android:title="@string/SYNC_MANAGER" />
        <item
            android:id="@+id/block_button"
            android:icon="@drawable/ic_menu_lock"
            android:title="@string/set_pin" />

        <item
            android:id="@+id/logout_button"
            android:icon="@drawable/ic_menu_logout"
            android:title="@string/log_out" />

    </group>

    <group
        android:id="@+id/extra_section"
        android:checkableBehavior="single">


        <item
            android:id="@+id/menu_about"
            android:icon="@drawable/ic_menu_info"
            android:title="@string/about" />

    </group>

    <group
        android:id="@+id/account_section"
        android:checkableBehavior="single">

    </group>
</menu>